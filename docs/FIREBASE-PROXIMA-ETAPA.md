# Próxima etapa: Firestore

Cole a configuração do Firebase no arquivo `assets/js/firebase.example.js` quando formos migrar.

Coleções prováveis:

- `lancamentos`
- `produtos`
- `atendimentos`

A lista de fiados e relatórios pode continuar sendo calculada a partir de `atendimentos` e `lancamentos`.
